# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=volteec_user
DATABASE_PASSWORD=volteec_password
DATABASE_NAME=volteec_db

# Database TLS Mode (OPTIONAL, default: require)
# Valid values:
#   - require: Enforce TLS/SSL (recommended for production)
#   - prefer: Try TLS, fallback to plaintext if unavailable
#   - disable: Plaintext only (NOT recommended for production, dev only)
# DATABASE_TLS_MODE=require

# API Authentication (REQUIRED)
# Generate a secure random token for production use
# Example: openssl rand -base64 32
API_TOKEN=your_secure_api_token_here

# Device Token Encryption (REQUIRED)
# AES-256 key (base64, 32 bytes). Generate: openssl rand -base64 32
DEVICE_TOKEN_KEY=your_device_token_key_here

# Relay Configuration (optional)
# If RELAY_URL is set, all other RELAY_* variables are required.
# RELAY_URL=https://relay.your-domain.com/v1
# RELAY_TENANT_ID=your_tenant_id
# RELAY_TENANT_SECRET=your_tenant_secret
# RELAY_SERVER_ID=your_server_uuid
# RELAY_ENVIRONMENT=sandbox

# Backend Versioning (optional)
# BACKEND_PROTOCOL_VERSION=1.1
# BACKEND_SOFTWARE_VERSION=1.1.0

# NUT Configuration (optional; enables polling)
# NUT_HOST=192.168.1.100
# NUT_UPS=ups1,ups2
# NUT_PORT=3493
# NUT_USERNAME=
# NUT_PASSWORD=
# NUT_POLL_INTERVAL=1.0
